package Shape;

// Interfaces forces a contract like relationship with classes that implements this interface
public interface ShapeI {
	
	public double calculateArea();
	public double calculatePerimeter();
	
}
